import classNames from "classnames";
import React from "react";
import styles from "./styles.module.scss"; // Adjust the path as necessary
const Skills = () => {
  return (
    <div className={classNames(styles["skills"])} id="skills">
      Skills section
    </div>
  );
};

export { Skills };
